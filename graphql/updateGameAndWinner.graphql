 mutation updateGameAndWinner(
    $gameId: ID!
    $winnerId: ID!
    $spread: Float
  ) {
    updateGame(
      where: { id: $gameId }
      data: { winner: { connect: { id: $winnerId } }, spread: $spread }
    ) {
      id
      winner {
        id
      }
      spread
    }
  }