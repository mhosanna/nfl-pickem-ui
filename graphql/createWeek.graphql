mutation createWeek(
    $label: String
    $slug: String
    $season: String
  ) {
    createWeek(data: { label: $label, slug: $slug, season: $season }) {
      id
      label
      slug
      season
    }
  }