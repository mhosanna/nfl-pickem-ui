mutation createWeek(
    $label: String
    $slug: String
    $season: String
  ) {
    createWeek(data: { label: $label, slug: $slug, season: $season }) {
      __typename
      id
      label
      slug
      season
      gamesCount
      games{
        id
      }
    }
  }